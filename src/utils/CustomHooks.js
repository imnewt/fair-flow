import {useEffect, useRef} from 'react';

// CUSTOM HOOKS
module.exports.useDidMountEffect = (func, deps) => {
  const didMount = useRef(false);

  useEffect(() => {
    if (didMount.current) func();
    else didMount.current = true;
  }, deps);
};
