import React from 'react';
import {Text, View} from 'react-native';
import {BaseContainer, TabTitle} from '../components/CustomCoreComponents';

const Profile = ({params}) => {
  return (
    <BaseContainer>
      <TabTitle title="edit profile" />
    </BaseContainer>
  );
};

export default Profile;
